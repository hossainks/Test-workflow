name: Baseline Coverage

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-baseline:
    uses: hossainks/shared-ci/.github/workflows/mail-coverage.yaml@main
    # secrets:
    #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
